# 参与贡献

在独立分支修改代码，运行 README 中的全部开发检查，再提交 pull request。

- 协议行为变化必须附带使用合成数据的测试；使用 `httpx.MockTransport`，不请求真实校园网。
- 不提交原始抓包、完整登录 URL、密码、Cookie、MAC、IP 会话参数或真实用户信息。
- 保持 CLI 的退出码与 JSON 契约可预测；行为变化同步更新 README 和 CHANGELOG。
- 不在错误消息中插入原始 HTTP 异常或门户响应，它们可能包含凭据。
- 使用 `uv add` / `uv add --dev` 管理依赖，并提交更新后的 `uv.lock`。
- 新增其他学校的支持前，明确该部署的认证差异；不要只替换地址后宣称兼容。

报告问题请包括 Python 版本、`edunet --version`、系统、是否在校园网环境、脱敏命令及输出。优先提供最小化模拟响应；不要公开上传真实抓包。
